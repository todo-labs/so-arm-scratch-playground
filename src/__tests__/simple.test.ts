import { describe, expect, it } from "vitest";

// Simple test to check if path aliases work
describe("Path Alias Test", () => {
  it("should work with relative paths", () => {
    expect(true).toBe(true);
  });
});
